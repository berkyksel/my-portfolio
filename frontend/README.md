# Berk Yüksel Portfolio

Next.js portfolio site with a static iletişim section.

## Local development

```bash
npm install
npm run dev
```

## Production deployment

1. Import the `frontend` directory into Vercel (or deploy it as the project root).
2. Deploy with `npm run build`; Vercel uses `npm run build` automatically.
